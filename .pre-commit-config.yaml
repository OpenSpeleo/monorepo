# Root hooks validate monorepo orchestration only. Every subtree is excluded
# from these hooks because its own .pre-commit-config.yaml remains
# authoritative. .prekignore additionally prevents Ariane and Compass project
# discovery; their hooks must be run from inside those standalone directories.
exclude: >-
  (?x)^(
    apps/mobile/
    |apps/web/
    |apps/compass_sidecar/
    |apps/ariane_plugin/
    |packages/python/ariane_lib/
    |packages/python/compass_lib/
    |packages/python/mnemo_lib/
    |packages/python/openspeleo_core/
    |packages/python/openspeleo_lib/
  )

repos:
  # -------------------------
  # Generic sanity checks
  # -------------------------
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-json
      - id: check-toml
      - id: check-xml
      - id: check-yaml
        args: [--allow-multiple-documents]
      - id: debug-statements
      - id: check-builtin-literals
      - id: check-case-conflict
      - id: check-docstring-first
      - id: detect-private-key

  # -------------------------
  # Markdown formatting
  # -------------------------
  - repo: https://github.com/rbubley/mirrors-prettier
    rev: v3.9.5
    hooks:
      - id: prettier
        name: prettier markdown
        types_or: [markdown]
        args: [--prose-wrap=always]
        # args: [--prose-wrap=always, --embedded-language-formatting=off]
