# Shared Python packages

Python packages shared by multiple applications in this directory.

Each package must have its own `pyproject.toml`; the root `pyproject.toml` file
will need to be updated to include each of them.

Application-specific code remains in the owning subtree under `apps/`.
